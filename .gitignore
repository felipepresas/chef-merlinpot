# dependencias
/node_modules
/.pnp
.pnp.*

# next
/.next/
/out/
next-env.d.ts

# producción
/build

# entorno (nunca commitear secretos)
.env
.env*.local

# claude — settings locales con secretos (token Dokploy, etc.)
.claude/settings.local.json

# prisma
/prisma/*.db

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editores / SO
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea

# tipos / caché
*.tsbuildinfo
/coverage

# volcados temporales de la API de Dokploy (pueden llevar secretos)
_dk_*
